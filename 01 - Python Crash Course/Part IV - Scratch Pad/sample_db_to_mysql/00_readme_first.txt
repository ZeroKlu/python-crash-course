This process uses my sm_utils library for relative paths

You will need to install the sm_utils library using the following command:
    py -m pip install --index-url https://test.pypi.org/simple/ --no-deps sm_utils==0.0.4